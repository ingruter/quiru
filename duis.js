// Example array
const array = [10, 20, 30, 40, 50, 60, 70];

// Initialize index
let index = 0;

// Loop through the array, incrementing index by 2 each time
while (index < array.length) {
  // Process the current element
  console.log(array[index]);

  // Increment index by 2
  index += 2;
}
